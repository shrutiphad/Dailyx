# deps
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
.turbo/

# env & secrets — never commit these
.env
.env.*
!.env.example
*.pem

# local-only docker port/config overrides (machine-specific)
docker-compose.override.yml

# prisma
packages/db/prisma/*.db

# misc
.DS_Store
*.log
coverage/
